{% extends 'base.html.twig' %}

{% block title %}Modifier le projet{% endblock %}

{% block body %}
    <main class="admin__edit">
        <h1 class="admin__title">Modifier le projet</h1>

        <div class="actions">
            <a class="actions__return" href="{{ path('app_project_index') }}">Retour à la liste</a>
        </div>

        {{ include('admin/project/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="actions">
            {{ include('admin/project/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
