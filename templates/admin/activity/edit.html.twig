{% extends 'base.html.twig' %}

{% block title %}Modifier l'activité{% endblock %}

{% block body %}
    <main class="admin__edit">
        <h1 class="admin__title">Modifier l'activité</h1>

        <div class="actions">
            <a class="actions__return" href="{{ path('app_activity_index') }}">Retour à la liste</a>
        </div>

        {{ include('admin/activity/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="actions">
            {{ include('admin/activity/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
