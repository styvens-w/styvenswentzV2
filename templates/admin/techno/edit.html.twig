{% extends 'base.html.twig' %}

{% block title %}Modifier la techno{% endblock %}

{% block body %}
    <main class="admin__techno__edit">
        <h1 class="admin__title">Modifier la techno</h1>

        <div class="actions">
            <a class="actions__return" href="{{ path('app_techno_index') }}">Retour à la liste</a>
        </div>

        {{ include('admin/techno/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="actions">
            {{ include('admin/techno/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
